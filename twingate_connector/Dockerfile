FROM twingate/connector:latest

ENV TWINGATE_NETWORK=${TWIN_GATE_NETWORK}
ENV TWINGATE_ACCESS_TOKEN=${ACCESS_TOKENS}
ENV TWINGATE_REFRESH_TOKEN=${REFRESH_TOKEN}
ENV TWINGATE_LABEL_HOSTNAME=homelabPi1
ENV TWINGATE_LABEL_DEPLOYED_BY=docker

CMD ["ash", "-c", "while true; do sleep 1000; done"]